Extension mechanism - creating new portal container
----------------------------------------------------------------------

You should access it after typing: http://localhost:8080/groundwork-portal in your browser.

 Datasource is at parent folder (groundwork-ds.xml).
 

To run on EPP5, copy the EAR and datasource and in the gatein-ds.xml rename the default JNDI datasources - add suffix '_portal' and start

