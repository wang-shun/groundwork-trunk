#!/usr/bin/perl

package GWNMSInstaller::UI::GWMDropdownMenu;
@ISA = qw(GWInstaller::UI::DropdownMenu);

sub make_new{
	my $self = shift;
	my $popupbox = $self->{popupbox};
	my $cui = $self->{cuisc}
	
}