The project.xml file is not maintained in Subversion for this application.
Rather, it is constructed on-the-fly from the project.xml.template file
located here, when the RPM-based build of this application is initiated from
the ../../../monitor-cloud/ directory via a "make clean rpms" command.
